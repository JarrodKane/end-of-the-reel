import React from "react";

interface Props {}

const ModalTest = (props: Props) => {
  return (
    <div className={`relative `}>
      <div className={`fixed top-14 z-30 bg-red-500 h-screen w-screen`}>df</div>
    </div>
  );
};

export default ModalTest;
